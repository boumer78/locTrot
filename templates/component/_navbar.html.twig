<!-- Header -->

<section id="one">
<body class="landing">
<header id="header" class="alt">
    <h1><a href="{{ path('home_page') }}" class="logo">Loc Ta Trott'</a></h1>
    <a href="#nav">Menu</a>
</header>
</section>
<!-- Nav -->

<nav id="nav">
    <ul class="links">

        <li><a href="home">Accueil</a></li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li>
                <a href="{{ path('security_deconnexion') }}">Deconnexion</a>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a href="{{ path('scooter') }}">Administration</a>
                </li>
            {% endif %}
        {% else %}
            <li>
                <a href="{{ path('security_connexion') }}">Connexion</a>
            </li>
            <li>
                <a href="{{ path('new_clients') }}">Inscription</a>
            </li>

        {% endif %}
        <li><a href="">Gestion Profil</a></li>
        <li><a href="/offers">Nos offres</a></li>
        <li><a href="{{ 'contact' }}">Contact</a> </li>
    </ul>
</nav>